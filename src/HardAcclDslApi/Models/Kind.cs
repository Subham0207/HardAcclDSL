namespace HardAcclDslApi.Models
{
    public enum Kind
    {
        Keyword,
        Identifier,
        Number,
        String,
        Operator,
        Punctuator,
        EOF,
        Invalid
    }
}